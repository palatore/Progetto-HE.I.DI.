import { API_URL } from "src/app/api-config";

export const environment = {
  production: true,
  apiUrl: API_URL //Se avviato da android sosttuire 'localhost' con l'indirizzo ip della macchina usata per hostare
};
